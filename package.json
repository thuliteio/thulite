{
  "name": "hyas",
  "private": true,
  "browserslist": [
    "defaults"
  ],
  "scripts": {
    "start": "hugo server",
    "build": "hugo --minify && npm run build:functions",
    "build:functions": "netlify-lambda build assets/lambda",
    "build:preview": "npm run build -D -F",
    "server": "hugo server",
    "clean": "rimraf public resources functions",
    "lint": "npm run -s lint:scripts && npm run -s lint:styles",
    "lint:scripts": "eslint assets/js assets/lambda",
    "lint:styles": "stylelint \"assets/sass/**/*.{css,sass,scss,sss,less}\"",
    "test": "npm run -s lint"
  },
  "devDependencies": {
    "@fullhuman/postcss-purgecss": "^1.3.0",
    "autoprefixer": "^9.7.2",
    "eslint": "^6.7.1",
    "netlify-lambda": "^1.6.3",
    "postcss-cli": "^6.1.3",
    "rimraf": "^3.0.0",
    "stylelint": "^12.0.0",
    "stylelint-config-standard": "^19.0.0"
  },
  "dependencies": {
    "bootstrap": "^4.4.0"
  }
}
